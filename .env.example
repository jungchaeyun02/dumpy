# 데이터베이스 (PostgreSQL)
#
# 연결이 두 개 필요하다. 같은 DB지만 들어가는 문이 다르다:
#   DATABASE_URL - 앱용. 풀링되는 쪽
#   DIRECT_URL   - 마이그레이션용. 트랜잭션·advisory lock 이 그대로 넘어가야 하는 쪽
#
# Supabase (프로젝트 > Connect 에서 복사)
# 앱용 - transaction 모드 풀러 (6543). pgbouncer=true 를 꼭 붙인다
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true"
# 마이그레이션용 - session 모드 풀러 (5432)
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres"
#
# Neon 을 쓸 경우 (호스트에 -pooler 가 들어간 쪽이 앱용)
# DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
# DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# JWT 세션 토큰 서명 키 (토스 미니앱용)
JWT_SECRET="your-jwt-secret-key-here-min-32-chars"

# 세션 쿠키 시크릿 (웹용)
SESSION_SECRET="your-session-secret-key-here"

# 토스 앱인토스 설정 (미니앱용)
TOSS_APP_ID="your-toss-app-id"
TOSS_API_SECRET="your-toss-api-secret"

# mTLS 인증서 - /api/auth/toss/verify 가 쓰는 값. 절대 Git에 올리지 말 것.
#
# 파일 경로가 아니라 PEM 내용을 통째로 넣는다. Vercel 은 배포마다 파일시스템이
# 새로 뜨기 때문에 경로를 넣어봐야 그런 파일이 없다.
#
# Vercel 대시보드에서는 PEM 을 여러 줄 그대로 붙여넣으면 된다.
# 한 줄로 넣어야 하는 도구에서는 줄바꿈을 \n 두 글자로 바꿔 넣어도 되고,
# 서버가 둘 다 알아서 처리한다.
TOSS_MTLS_CERT="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"
TOSS_MTLS_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# 개인키에 암호가 걸려 있을 때만. 안 걸려 있으면 넣지 않는다.
# TOSS_MTLS_KEY_PASSPHRASE=""

# (구) 파일 경로 방식 - 예전 /api/auth/toss/callback 만 참조한다.
# 새 verify 경로는 위의 TOSS_MTLS_CERT/KEY 를 쓴다.
TOSS_MTLS_CERT_PATH="/path/to/cert.pem"
TOSS_MTLS_KEY_PATH="/path/to/key.pem"

# 미니앱에서 이 서버의 API 를 부를 수 있는 출처. 쉼표로 구분.
# 끝에 / 를 붙이지 말 것 (붙어도 서버가 떼지만, 원래 Origin 헤더에는 없다)
TOSS_ALLOWED_ORIGINS="https://memoindumpy.apps.tossmini.com,https://memoindumpy.private-apps.tossmini.com"

# --- 임시 테스트 로그인 (토스 인증 붙기 전까지만) ---
# Preview/Development 에만 넣는다. Production 에는 절대 넣지 말 것.
# 값이 없으면 /api/auth/toss/test 는 401 로 거부한다 (값은 아무 문자열이나 된다)
ALLOW_TOSS_TEST="1"
# 그 통로로 로그인되는 고정 테스트 사용자의 식별자
TOSS_TEST_USER_ID="toss_test_user"

# 카카오 로그인 (웹용)
KAKAO_CLIENT_ID="your-kakao-client-id"
KAKAO_CLIENT_SECRET="your-kakao-client-secret"
KAKAO_REDIRECT_URI="https://your-domain.com/api/auth/kakao/callback"

# 앱 설정
NEXT_PUBLIC_APP_URL="https://your-domain.com"
NODE_ENV="development"

# 관리자 계정 User.id (이 id 로 로그인한 사용자만 관리자 권한)
ADMIN_USER_ID="your-admin-user-cuid"
